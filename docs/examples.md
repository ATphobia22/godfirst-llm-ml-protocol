# Examples

## Simple inference

See `examples/simple_inference.py` for a minimal example of wrapping a dummy backend.

## Audit demo

See `examples/audit_demo.py` to inspect the audit log and see how records are written.

## Safety layer demo

See `examples/safety_layer_demo.py` to observe how youth shield and truth guard modify or block outputs.
